import { OrbitProgress } from "react-loading-indicators";

const loadingOrbitBar: React.FC = () => {

    return (
        <>
            <OrbitProgress variant="dotted" color="#ff496c" size="medium" text="" textColor="" />
        </>
    );
}

export default loadingOrbitBar;
